programa {
  funcao inicio() {
    real a, b, c

    escreva("qual a base do triangulo? ")
    leia(a)

    escreva("qual a altura do triangulo? ")
    leia(b)
     c = (a*b)/2

     escreva("a area do triangulo é: ", c)
  }
}
